# Native execution; Compose fixes the DB location inside its named volume.
NATALIA_DB_PATH=data/natalia.db
NATALIA_MAX_WORKERS=2
# Only needed to administer local Grafana. Anonymous viewing is enabled on loopback.
GRAFANA_ADMIN_PASSWORD=local-change-me
